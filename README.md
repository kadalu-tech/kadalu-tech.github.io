## To Customize Bulma CSS styles

Download Bulma CSS.

```
unzip bulma-0.9.4.zip
mv bulma bulma-0.9.4
```

Install `sass` by running `gem install sass`

Modify `sass/stylesheet.scss` as required, then run

```
make gen-css
```
